# Leave blank to run in local demo mode (no backend needed)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — natural language parsing / scheduling falls back
# to a local implementation when this is blank
VITE_AI_PROVIDER=
